type ChatUser {
  id: ID!
  socketId: ID!
  userId: ID!
  latitude: Float!
  longitude: Float!
}

type Person {
  distance: Float!
  user: User!
  chatUser: ChatUser!
}

type Query {
  findPeople: [Person!]!
}
